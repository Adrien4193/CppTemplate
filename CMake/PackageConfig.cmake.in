@PACKAGE_INIT@

foreach(COMPONENT ${${CMAKE_FIND_PACKAGE_NAME}_FIND_COMPONENTS})
    include(
        ${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@${COMPONENT}Targets.cmake
        OPTIONAL
        @PROJECT_NAME@_${COMPONENT}_FOUND
    )
endforeach()

check_required_components(@PROJECT_NAME@)
